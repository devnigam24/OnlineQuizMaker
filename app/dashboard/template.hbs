{{#if isSIgnedIn}}
    {{profile-info userInsession=userInsession}}
    {{#if userInsession.isStudent}}
        {{#each model.data as |result|}}
            {{result-viewer result=result}}
        {{/each}}
    {{else}}
        {{#each model.data as |report|}}
            {{report-viewer report=report user=userInsession.emailId}}
        {{/each}}
    {{/if}}
{{/if}}
