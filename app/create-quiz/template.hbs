{{#if isSIgnedIn}}
    {{server-error serverSideFormError=serverSideFormError}}
    {{
      quiz-create
      addQuestions=(action "addQuestions")
    }}
    {{component addQuestionComponent quizObject=quizObject}}
{{/if}}
